server {
    listen 80;

    location / {
        # 'app' es el nombre del servicio en docker-compose / render
        proxy_pass http://app:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}